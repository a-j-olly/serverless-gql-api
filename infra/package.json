{
	"name": "aws",
	"version": "1.0.1",
	"description": "The terraform module used to deploy the application's infrastructure",
	"scripts": {
		"build": "node scripts/build.mjs",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"graphql_api": "workspace:*",
		"db_migration": "workspace:*",
		"esbuild": "^0.23.1"
	}
}
